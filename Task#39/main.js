// creating a function with parameters with returns a vallue with string
function city_name(city, country) {
    return "".concat(city, " ").concat(country);
}
// calling a fuunction and print the returned value
console.log(city_name("Karachi", "pakistan"));
console.log(city_name("Tokyo", "Japan"));
console.log(city_name("Berlin", "Germany"));
