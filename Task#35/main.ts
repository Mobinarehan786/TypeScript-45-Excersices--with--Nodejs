import { log } from "console";

let petAnimals =["cat","Dog","Rabbit"]
// using for loop

for(let onePet of petAnimals){
    console.log(`A ${onePet} would make agreat pet.`);
    console.log(
        "Any of these animals would make a great pet!"
    );
    
    
}
// Print amessage without for loop
